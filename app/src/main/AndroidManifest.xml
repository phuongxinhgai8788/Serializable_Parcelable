<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.serializablesample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SerializableSample">
        <activity
            android:name=".ParcelableActivity2"
            android:exported="false" />
        <activity
            android:name=".ParcelableActivity"
            android:exported="false" />
        <activity
            android:name=".SerializableActivity2"
            android:exported="false" />
        <activity
            android:name=".SerializableActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>